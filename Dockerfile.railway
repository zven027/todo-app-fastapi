# Railwayでも使える軽量Pythonイメージ
FROM python:3.10-slim

# アプリの作業ディレクトリを指定
WORKDIR /app

# 必要なファイルをすべてコピー
COPY . /app

# 依存ライブラリのインストール
RUN pip install --upgrade pip
RUN pip install -r requirements.txt

# Railwayでは環境変数PORTが動的に割り当てられるため、${PORT:-8000}で対応
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "${PORT}"]